apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-frontend
  labels:
    app: guestbook
    tier: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: guestbook
      tier: frontend
  template:
    metadata:
      labels:
        app: guestbook
        tier: frontend
    spec:
      containers:
      - name: guestbook-frontend
        # ⚠️ IMAGEN INICIAL: El pipeline la actualizará con el tag :SHA
        image: mikenarbikes/guestbook-frontend:82f674692fa6bc4447de5843a0497abf411bc837
        ports:
        - containerPort: 5000 
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
        env:
        - name: REDIS_MASTER_SERVICE_HOST
          value: redis-master # Nombre del servicio de Redis en K8s
        - name: REDIS_MASTER_SERVICE_PORT
          value: "6379"