apiVersion: v1
kind: Service
metadata:
  name: guestbook-frontend
  labels:
    app: guestbook
    tier: frontend
spec:
  type: LoadBalancer 
  ports:
  - port: 80
    targetPort: 5000 
  selector:
    app: guestbook
    tier: frontend